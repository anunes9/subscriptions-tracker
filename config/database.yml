# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # Connect as a non-superuser role in every environment, matching production.
  # Postgres superusers always bypass Row-Level Security regardless of policy —
  # connecting as "root" locally would silently make RLS a no-op in dev/test.
  # host+password (not the default local peer-auth socket) because peer auth
  # requires the OS user and Postgres role to share a name.
  username: subscriptions_tracker
  password: <%= ENV.fetch("LOCAL_DATABASE_PASSWORD", "subscriptions_tracker_dev") %>
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

# Solid Queue uses a second database role so its writes/locking stay isolated from
# the app's primary tables, per the architecture doc. It's still the same Postgres
# server/credentials — just a second logical database, not a second service to run.
development:
  primary:
    <<: *default
    database: subscriptions_tracker_development
  queue:
    <<: *default
    database: subscriptions_tracker_development_queue
    migrations_paths: db/queue_migrate

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  primary:
    <<: *default
    database: subscriptions_tracker_test
  queue:
    <<: *default
    database: subscriptions_tracker_test_queue
    migrations_paths: db/queue_migrate

# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
#
# Instead, provide the password or a full connection URL as an environment
# variable when you boot the app. For example:
#
#   DATABASE_URL="postgres://myuser:mypass@localhost/somedatabase"
#
# If the connection URL is provided in the special DATABASE_URL environment
# variable, Rails will automatically merge its configuration values on top of
# the values provided in this file. Alternatively, you can specify a connection
# URL environment variable explicitly:
#
#   production:
#     url: <%= ENV["MY_APP_DATABASE_URL"] %>
#
# Read https://guides.rubyonrails.org/configuring.html#configuring-a-database
# for a full overview on how database connection configuration can be specified.
#
production:
  primary:
    <<: *default
    database: subscriptions_tracker_production
    username: subscriptions_tracker
    password: <%= ENV["SUBSCRIPTIONS_TRACKER_DATABASE_PASSWORD"] %>
  queue:
    <<: *default
    database: subscriptions_tracker_production_queue
    username: subscriptions_tracker
    password: <%= ENV["SUBSCRIPTIONS_TRACKER_DATABASE_PASSWORD"] %>
    migrations_paths: db/queue_migrate
