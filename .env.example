# Database (optional — these already match docker-compose.yml's defaults,
# see README.md#1-postgres, so most people won't need to set them).
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
LOCAL_DATABASE_PASSWORD=subscriptions_tracker_dev

# Frontend product analytics (optional — client-side code no-ops without this).
# See README.md#observability.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
